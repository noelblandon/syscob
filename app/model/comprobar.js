$(document).ready(function(){
  /*var venta = 4;
   if($("#recibo").val().length > 0){ venta = venta-1;}
   if($("#fventa").val().length > 0){ venta = venta-1;}
   if($("#icant").val().length > 0){ venta = venta-1;}
   if($("#total").val().length > 0){ venta = venta-1;}
   if(venta == 0){
       $('#venta').attr('disabled', false);
   } else {
       $('#venta').attr('disabled', true);
   }*/


    //$(document).on('click', "#inventario", function (e){/*ActualizaProveedor*/
      //$('#inventario').attr('disabled', true);
        /* var producto = 5;
          if($("#prod").val().length > 0){ producto = producto-1;}
          if($("#precioc").val().length > 0){ producto = producto-1;}
          if($("#preciov").val().length > 0){ producto = producto-1;}
          if($("#ganancias").val().length > 0){ producto = producto-1;}
          if($("#cant").val().length > 0){ producto = producto-1;}
          if(producto == 0){
              $('#inventario').attr('disabled', false);
          } else {
              $('#inventario').attr('disabled', true);
          }
  	//});*/
});/*fin del documet*/
